#!/usr/bin/env bash

cat << EOF
Logging of the plugin itself is configured via environment variables.

LOGSH_COLOR:
  Can either be 'true' or 'false' and defaults to 'true'.

LOGSH_LEVEL:
  Integer ranging from 0 to 4. Defaults to 3. If the log
  message level is lower or equal to the selected level,the
  log message wil be written to stderr. Here are the mappings
  from integers to text levels:

  0=ERROR
  1=WARNING
  2=SUCCESS
  3=INFO
  4=DEBUG
EOF
